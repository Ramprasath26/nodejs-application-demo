apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: nodejs-application-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            backend:
              serviceName: nodejs-application-svc
              servicePort: 3000
